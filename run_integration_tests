cd test && npm start &
SERVER_PID=$!

until $(curl --output /dev/null --silent --head --fail http://localhost:3000); do
    printf '.'
    sleep 5
done

NODE_ENV=integration ./node_modules/.bin/ghostjs --ghost-runner slimerjs $(find src -name '*.ghost.js')
kill -9 $SERVER_PID || true
